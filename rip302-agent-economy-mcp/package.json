{
  "name": "rip302-agent-economy-mcp",
  "version": "0.1.0",
  "description": "MCP server and CLI for the RustChain RIP-302 Agent Economy API.",
  "type": "module",
  "bin": {
    "rip302-agent": "dist/cli.js",
    "rip302-agent-mcp": "dist/mcp.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test tests/*.test.js",
    "pretest": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.15.0",
    "zod": "^3.25.7"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
